# Mock Authentik

This directory contains mock Authentik server implementations for testing.

## Purpose

Provides mock Authentik server functionality for unit testing without requiring a real Authentik instance.
